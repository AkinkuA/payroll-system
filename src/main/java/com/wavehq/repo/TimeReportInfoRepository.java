package com.wavehq.repo;

import com.wavehq.model.TimeReportInfo;
import org.springframework.data.repository.CrudRepository;

public interface TimeReportInfoRepository extends CrudRepository<TimeReportInfo, Integer> {
}
